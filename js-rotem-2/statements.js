// Statements - do not have values (usually..)

// conditional - "if" statement
if (true) {} else {}

// "switch" statement
switch (true){
	case true:
		break
}

// loops

// iteration - מחזור פעולה
// itar = for
for (let i = 0; i < array.length; i++)
{
	const arrayElement = array[i];
}

// iter - for..of
for (const arrayElement of array) // arrayElement is the element in the array
{
}

// itin - for..in
for (const arrayKey in array) // arrayKey is the key in the array
{
}

// while loop
while (1===2) {
	//...
}

do {} while (1===1)

// variable declarations
var x = 1;
let y = 2;
const r = 10;
